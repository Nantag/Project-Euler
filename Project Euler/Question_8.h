#pragma once

// Finds the longest sequence of 13 adjacent numbers in a 1000 number sequence.
long long Question_8 ();

// Question requires interface and return type type switch to long long to not overflow.